---
- name: "Build Docker Images"
  shell: | 
    cd app && cd project && docker-compose build

- name: "Push Images to DockerHub"
  shell: | 
    cat ~/my_password.txt | docker login --username "{{ lookup('env', 'USERNAME') }} --password-stdin && docker-compose push
